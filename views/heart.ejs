<html>
    <head>

        <link rel="stylesheet" type="text/css" href="/css/homepage.css" />
        
<meta charset="UTF-8">

        <script type = "text/javascript" src="/js/homepage.js" > </script>

    </head>
    <body>
        <nav class="navbar">
            <a href="/">Home</a>
            <a href="/journals">Journals</a>
         
        </nav>

        <% if(heartData == null){ %>
            <p>None Avaliable</p>
        <% }else { %>
    
            
                <% for (value of heartData) { %>
                    <p>value.dateTime</p>
                    <% for (heartratezones of value.value.heartRateZones) { %>   
        <div>
            
            <p>heartratezones.max</p>
        </div>
                    

        <% } %>
                <% } %>
 

            <% } %>
        
    </body>
</html>
